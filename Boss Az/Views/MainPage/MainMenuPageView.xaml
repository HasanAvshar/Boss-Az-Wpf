<Page x:Class="Boss_Az.Views.MainPage.MainMenuPageView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:desing="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="800"
      Title="MainMenuPageView" Background="White" ShowsNavigationUI="False" Name="Page">

    <Grid>
        <StackPanel HorizontalAlignment="Center"  VerticalAlignment="Center">
            <Button Content="Employee" Command="{Binding EmployeeCommand}" CommandParameter="{Binding ElementName=Page}" Margin="10" Style="{DynamicResource MaterialDesignOutlinedDarkButton}" FontSize="18"/>
            <Button Content="Worker" Margin="10" Command="{Binding WorkerCommand}" CommandParameter="{Binding ElementName=Page}" Style="{DynamicResource MaterialDesignOutlinedDarkButton}" FontSize="18"/>
        </StackPanel>
    </Grid>
</Page>
